package Hashing.OpenAddresing;

public class DoubleHashig {
    public static void main(String[] args) {
        /*
        Double Hashing - hash(key i) = (h1(key)+ih2(key))%m.
        if h2(key) is relatively prime to m, than it always find a free slot if there is one.
        Distribute keys more uniformly than linear probing and quadratic hashing.
        No clustering
         */
    }
}
